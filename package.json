{
  "name": "hybrid-messenger-localhost",
  "version": "8.0.0",
  "private": true,
  "description": "Localhost messenger v8 with Telegram-like layout, circles, themes and basic WebRTC calls.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node --check server.js && node --check public/app.js",
    "docker:build": "docker build -t velora ."
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^12.8.0",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
